#include "monty.h"

/**
 * f_stackk -this prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/

void f_stackk(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
